import { H1 } from '@venkatasudha/core-components';

/* eslint-disable-next-line */
export interface ProjectsProps {}

export function Projects(props: ProjectsProps) {
  return <H1 pageTitle={'Projects'} />;
}

export default Projects;
