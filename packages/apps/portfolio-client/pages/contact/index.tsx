import { H1 } from '@venkatasudha/core-components';

/* eslint-disable-next-line */
export interface ContactProps {}

export function Contact(props: ContactProps) {
  return <H1 pageTitle={'Contact'} />;
}

export default Contact;
