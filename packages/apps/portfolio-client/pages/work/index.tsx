import { H1 } from '@venkatasudha/core-components';

/* eslint-disable-next-line */
export interface WorkProps {}

export function Work(props: WorkProps) {
  return <H1 pageTitle={'Work'} />;
}

export default Work;
